# Development Environment Configuration
# This file is for local development with Docker Compose

# Application
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3000

# Database (Docker Compose PostgreSQL)
DATABASE_URL=postgresql://todo_user:todo_password_dev@localhost:5432/todo_nestjs_dev
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# JWT Secrets (Development only - generate secure keys for production)
JWT_ACCESS_SECRET=dev-access-secret-change-in-production-f8a3b2c1d4e5
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production-a1b2c3d4e5f6
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Email Provider (Optional for development)
# Option 1: SendGrid (uncomment if you have an API key)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Option 2: SMTP (uncomment and configure)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your-mailtrap-user
# SMTP_PASS=your-mailtrap-pass
# SMTP_SECURE=false

# Email Settings
EMAIL_FROM=noreply@todo.local
EMAIL_FROM_NAME="TodoApp Dev"
EMAIL_REPLY_TO=support@todo.local

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:4200

# Dev Only - Reversible Passwords (for debugging)
ENABLE_DEV_REVERSIBLE_PASSWORDS=false
# DEV_REVERSIBLE_PASSWORD_KEY=generate-with-openssl-rand-base64-32

# Monitoring
ENABLE_METRICS=false
LOG_LEVEL=debug
